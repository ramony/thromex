version: '3.8'

services:
  thromeapi:
    build: .
    container_name: thromeapi
    restart: always
    ports:
      - "4001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_HOST=192.168.0.113
    volumes:
      # 开发热重载用，生产注释掉
      - ./src:/app/src
